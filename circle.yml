machine:
  xcode:
    version: 8.2

dependencies:
  pre:
    - gem install xcpretty --no-rdoc --no-ri --no-document --quiet
    - Scripts/install_swiftlint.sh

test:
  override:
    - rake lint:code
    - rake xcode:test
    - rake output:compile
    - rake lint:output
