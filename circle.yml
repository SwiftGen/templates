machine:
  xcode:
    version: 8.2

dependencies:
  pre:
    - gem install xcpretty --no-rdoc --no-ri --no-document --quiet
    - rake lint:install

test:
  override:
    - rake lint:tests
    - rake xcode:test
    - rake output:compile
    - rake lint:output
