// Generated using SwiftGen, by O.Halligon — https://github.com/SwiftGen/SwiftGen
import Foundation

{% if plistKeys %}
private let _Plist = Bundle.main.infoDictionary!
class _InfoPlist {
  subscript(key: InfoPlistKey) -> Any? {
    return _Plist["\(key)"]
  }
}
let InfoPlist = _InfoPlist()

enum InfoPlistKey: String {
  {% for key in plistKeys %}
  case {{key}} = "{{key}}"
  {% endfor %}
}
{% else %}
// No plistKey found
{% endif %}
